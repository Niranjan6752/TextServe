# TextServe
A multi-tier HTTP-based text analysis system with caching, database persistence, and load generation. Designed to demonstrate CPU-bound and IO-bound workload behavior under concurrent client requests.
